# Project-relative store so pnpm's store location is consistent everywhere:
# rootful/rootless Docker/Podman builds, non-root local users, and Heroku's
# non-root slug compiler (an absolute path like /root/... breaks all of those
# except the container-root case).
store-dir=.pnpm-store
